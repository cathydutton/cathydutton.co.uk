---
title: My Title
description: fwfew
pageName: My portfolio
pageDescription: A collection design and front end development work
layout: layouts/base.njk
permalink: /projects/
---


 <h3>The important bits</h3>

<ol>
  <li><strong>An opening statement,</strong> addressed to a particular person, introducing myself and stating clearly why I’d be a great fit for the position.</li>
  <li><strong>A collection of my work,</strong> specifically tailored to speak to this particular job description. For example, this job description uses specific phrases like “product presentation“ and “retail business targets,” so I used those same phrases in talking about my work.</li>
  <li><strong>Something special about me</strong> professionally or personally that sets me above everyone else applying for this job. I’m not just another designer; I’m a designer that’s also a sneakerhead.</li>
</ol>



{% for post in collections.projects %}
  <article class="project__post js-content-group" data-url="{{ post.url }}" data-title="{{post.data.title}}">
    <header class="project__header">
      <h3 class="project__title">
        <a href="{{ post.url }}">
          <span class="visually-hidden">Project title</span>
          {{ post.data.title }} 
        </a>
      </h3>
      <img src="{{ post.data.image }}" alt="{{ post.data.title }} image">
      <p>
        <span class="visually-hidden">Project date</span>
        <time datetime="{{ post.data.date }}">{{ post.data.date | dateDisplay }}</time>
      </p>
    </header>
    <div>
      {% if post.data.tags %}
        <p>Tagged in: {% for projectTag in post.data.tags %}<a href="/projects/tags/{{ projectTag | lower | replace(" ", "-") }}/">{{ projectTag }}</a>{% if not loop.last %}, {% endif %}{% endfor %}</p>
      {% endif %}
    </div>
  </article>
{%- endfor -%} 