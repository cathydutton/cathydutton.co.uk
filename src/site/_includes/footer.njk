
<nav class="navigation-footer">
  {% include "navigation.njk" %}
</nav>

<ul class="social-media">
  {% for item in socialMedia %} 
		<li class="social-media__item">
			<a href="{{ item.url }}" class="anchor social-media__anchor" aria-label="{{ item.title }}" target="_blank" rel="noopener">
			<span>{{ item.description }}</span>
				<svg version="1.1" width="100" height="100" role="img" viewBox="{{ item.viewBox }}" aria-labelledby="{{ item.title }} Title" aria-describedby="{{ item.title }}Desc">
					<title id="{{ item.title }}TitleFooter">{{ item.description }}</title>
   				<desc id="{{ item.title }}DescFooter">link to {{ item.title }} profile</desc>
			  	<path d="{{ item.path }}"/>
				</svg>
		 	</a>
		</li>
  {% endfor %}
</ul> 


<p>
  Proudly powered by <a href="//11ty.io" title="Powered by Eleventy" class="anchor" rel="noopener">Eleventy</a>. Hosted on <a href="https://www.netlify.com/" class="anchor"  title="Hosted on Netlify" rel="noopener">Netlify</a>, last updated  {{ date | timestamp }} - <a href="https://github.com/cathydutton/cathydutton.co.uk" rel="noopener" class="anchor" title="view source code on github">view on github</a>.
</p>